using System;
using System.Collections.Generic;
using System.Text;

using theori.Audio;

namespace theori
{
    // TODO(local): Make this `Host` when Host is factored out!!
    // TODO(local): Make this `Host` when Host is factored out!!
    // TODO(local): Make this `Host` when Host is factored out!!
    // TODO(local): Make this `Host` when Host is factored out!!
    // TODO(local): Make this `Host` when Host is factored out!!
    // TODO(local): Make this `Host` when Host is factored out!!
    // TODO(local): Make this `Host` when Host is factored out!!
    public abstract class TheMaster
    {
        public IAudioDevice AudioDevice { get; }

        protected TheMaster()
        {
        }
    }
}
